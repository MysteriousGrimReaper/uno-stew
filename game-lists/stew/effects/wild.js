const { ButtonBuilder, ActionRowBuilder, ButtonStyle } = require("discord.js");
module.exports = {
	name: `w`,
	text: `Wild`,
	wild: true,
	async effect() {
		// in case something should ever happen when a wild card is played
	},
};
